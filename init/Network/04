#!/bin/bash
netstat -rn | grep "Internet:" -A2 | grep "default" | awk '{print $2}'
#инструмент командной строки для мониторинга как входящих, так и исходящих сетевых 
#подключений, а такжепросмотра таблиц маршрутизации
