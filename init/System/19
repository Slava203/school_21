systemctl list-unit-files --type=service | grep "enabled" | awk -F'.service' '{print $1}'
# pstree -p - позволяет отобразить все процессы с начала старта системы  
# systemctl - позволяет работать с системными процессами
# 
# Сервис (служба, демон)- специальная программа, выполняющая определенные действия в 
# фоновом режиме. Например, демон печати ждет, пока какое-либо из приложений отправит 
# документ на печать. После этого демон активизируется и осуществляет требуемые 
# действия - печать самого документа на выбранном принтере.
